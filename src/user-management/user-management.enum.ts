export enum UserStatusType {
  active = 'active',
  inactive = 'inactive',
}

export enum MenuLevel {
  group = 'group',
  main = 'main',
  sub = 'sub',
}

export enum MenuUrlType {
  iframe = 'iframe',
  hyperlink = 'hyperlink',
}

export enum MenuStatusType {
  active = 'active',
  inactive = 'inactive',
}
